#include "main.h"

/**
* _isdigit - checks whether function input is a digit
*
* return 1 if input is a digit otherwise returns 0 
*/

int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}

	return (0);
}
